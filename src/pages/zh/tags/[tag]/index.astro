---
import { getAllTags } from '@/utils/posts';

export async function getStaticPaths() {
  const tags = await getAllTags('zh');
  return [...tags.keys()].map((tag) => ({
    params: { tag },
  }));
}

const { tag } = Astro.params;
return Astro.redirect(`/tags/${encodeURIComponent(tag!)}/`);
---
